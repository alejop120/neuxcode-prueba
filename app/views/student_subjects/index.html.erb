<p id="notice"><%= notice %></p>

<h1>Materias de <%= @student.name %></h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Calificación</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @student_subjects.each do |student_subject| %>
      <tr>
        <td><%= number_with_precision(student_subject.rate, precision: 1) %></td>
        <td><%= link_to "Calificar", edit_student_student_subject_path(@student, student_subject) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>